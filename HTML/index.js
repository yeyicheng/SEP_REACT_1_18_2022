// let element = document.querySelector('body');
// console.dir(element);

/// Javascript = ES + Web API
/// Ecmascript : ES5 ES6 ES7 | Syntax standard
/// NodeJS = ES + Node API

const fs = require('fs');
console.log(fs);
